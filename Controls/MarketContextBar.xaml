<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CryptoPortfolioTracker.Controls.MarketContextBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="OnLoaded"
    mc:Ignorable="d">

    <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            CornerRadius="8" Padding="12,5"
            ToolTipService.ToolTip="Gedeelde marktcontext: BTC-regime, Fear &amp; Greed-index en het eerstvolgende macro-event (FOMC/CPI/NFP/PCE).">
        <StackPanel Orientation="Horizontal" Spacing="14" VerticalAlignment="Center">
            <TextBlock Text="Marktcontext" FontSize="11" Opacity="0.6" VerticalAlignment="Center"/>

            <Border x:Name="RegimeBadge" CornerRadius="6" Padding="8,2" VerticalAlignment="Center">
                <TextBlock x:Name="RegimeTextBlock" Text="…" FontSize="12" FontWeight="SemiBold" Foreground="White"/>
            </Border>

            <TextBlock x:Name="FgTextBlock" Text="F&amp;G —" FontSize="12" VerticalAlignment="Center"/>

            <TextBlock x:Name="EventTextBlock" Text="" FontSize="12" Foreground="#E67E22"
                       VerticalAlignment="Center"/>

            <ProgressRing x:Name="Ring" IsActive="True" Width="14" Height="14" VerticalAlignment="Center"/>
        </StackPanel>
    </Border>
</UserControl>
